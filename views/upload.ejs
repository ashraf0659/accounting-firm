<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Upload Documents - Fine Services</title>
  <link rel="icon" href="/favicon1.png?v=2" type="image/png">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #f4f6f9;
      color: #333;
      line-height: 1.6;
    }

    /* ===== HEADER TITLE ===== */
    .page-header {
      background: #1a3c7c;
      color: #fff;
      text-align: center;
      padding: 40px 20px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
    }

    .page-header h2 {
      margin: 0;
      font-size: 28px;
    }

    /* ===== UPLOAD CARD ===== */
    .upload-box {
      background: #fff;
      max-width: 700px;
      margin: 40px auto;
      padding: 35px;
      border-radius: 12px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.08);
      text-align: center;
    }

    .upload-box h3 {
      margin-top: 0;
      color: #1a3c7c;
    }

    form {
      margin: 20px 0;
    }

    input[type="file"] {
      margin: 15px 0;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
    }

    button {
      padding: 12px 25px;
      background: #1a3c7c;
      color: #fff;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    button:hover {
      background: #4caf50;
      transform: translateY(-2px);
    }

    /* ====== UPI BOX ====== */
    .upi-box {
      margin-top: 30px;
      padding: 25px;
      border: 2px dashed #1a3c7c;
      border-radius: 10px;
      background: #f8fbff;
      text-align: center;
    }

    .upi-box h3 {
      color: #e63946;
      margin: 12px 0;
    }

    .upi-box p {
      margin: 6px 0;
    }

    .qr img {
      margin-top: 15px;
      width: 160px;
      height: 160px;
      border-radius: 8px;
      border: 1px solid #ddd;
      padding: 6px;
      background: #fff;
    }


    /* Success message */
    .success {
      color: green;
      font-weight: bold;
      margin-bottom: 15px;
      font-size: 16px;
    }

    /* Back link */
    a.back-home {
       display: inline-block;
      margin-top: 10px;
      padding: 10px 20px;
      background: #1a3c7c;
      color: #ffffff;
      text-decoration: none;
      font-weight: 600;
      border-radius: 6px;
      transition: background 0.3s ease;
    }

    a.back-home:hover {
      background: #4caf50;
    }

    /* ====== FOOTER ====== */
   footer {
      background-color: #1a3c7c;
      color: #f1f5f9;
      padding: 40px 20px;
      margin-top: 60px;
    }

    footer h3 {
      color: #a8e6a3;
      margin-bottom: 10px;
    }

    .footer-container {
      display: flex;
      justify-content: space-around;
      flex-wrap: wrap;
      text-align: left;
    }

    .footer-container div {
      flex: 1;
      min-width: 250px;
      margin: 10px;
    }

    footer hr {
      border: 0;
      border-top: 1px solid rgba(255, 255, 255, 0.08);
      margin: 20px 0;
    }

    footer p {
      text-align: left;
      margin: 0;
      opacity: 0.95;
    }
     footer p.center {
      text-align: center;
      margin: 0;
    }
  </style>
</head>

<body>

  <!-- Page Header -->
  <div class="page-header">
    <h2>Upload Documents for <%= inquiry.service %>
    </h2>
  </div>

  <!-- Upload Form -->
  <!-- <div class="upload-box"> -->
    <!-- <% if (message) { %>
      <p class="success">
        <%= message %>
      </p>
      <% } %> -->
<div class="upload-box">
  <% if (message) { %>
    <p class="success"><%= message %></p>
  <% } %>
  <% if (query.error) { %>
    <p style="color:red; font-weight:bold;"><%= query.error %></p>
  <% } %>


        <form action="/upload/<%= inquiry._id %>" method="POST" enctype="multipart/form-data">
          <input type="file" name="document" required>
          <br>
          <button type="submit">Upload</button>
        </form>

        <div class="upi-box">
          <p>Please make the payment to the following UPI ID:</p>
          <h3>shaikh.xaasharf.26@okhdfcbank</h3>
          <p>Amount will be communicated by our office.</p>
          <p>Scan this QR code to pay:</p>
          <div class="qr">
            <img src="/upi.jpeg" alt="UPI QR Code">
          </div>
        </div>

        <p style="margin-top:20px;">
          After payment, kindly visit our office or share the payment proof (screenshot) for acknowledgement.
        </p>

        <a href="/" class="back-home">⬅ Back to Home</a>
  </div>

  <!-- Footer -->
  <footer>
    <div class="footer-container">
      <div>
        <h3>Fine Services</h3>
        <p>
          Serving you with reliable accounting and tax services.
          Accuracy, trust, and professionalism in every service we deliver.
        </p>
      </div>
      <div>
        <h3>Contact Us</h3>
        <p><b>Phone:</b> +91 9372595680</p>
        <p><b>Email:</b> FineServicesIHS@gmail.com</p>
        <p><b>Address:</b> R. No.4, B-wing, Royal C.H.S., Opp Ginger Hotel, Castle mill, Thane(W), 400601</p>
      </div>
      <div>
        <h3>Opening Hours</h3>
        <p>Mon – Sat: 10:00 AM – 7:00 PM</p>
        <p>Sunday: Closed</p>
      </div>
    </div>
    <hr>
    <p class="center">© <%= new Date().getFullYear() %> Fine services All Rights Reserved.</p>
  </footer>

</body>

</html>